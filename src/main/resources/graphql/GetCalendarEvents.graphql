query GetCalendarEvents($startDate: String, $endDate: String) {
    getCalendarEvents(
        startDate: $startDate,
        endDate: $endDate
    ) {
        results {
            id
            name
            startDate
            endDate
            area
            venue
            disciplines
        }
    }
}